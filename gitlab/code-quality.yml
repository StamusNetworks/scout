linting:
  stage: code-quality
  image: node:24-alpine
  script:
    - npm i -g pnpm@9.15.2
    - pnpm install --frozen-lockfile
    - pnpm lint

typescript:
  stage: code-quality
  image: node:24-alpine
  script:
    - npm i -g pnpm@9.15.2
    - pnpm install --frozen-lockfile
    - pnpm check