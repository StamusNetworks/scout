services:
    web:
        build:
            context: .
            dockerfile: Dockerfile.local
        ports:
            - '5173:5173'
        volumes:
            - .:/code
            - ./public/config.local.json:/code/public/config.json
